# Backend — development (PostgreSQL w Dockerze na hoście localhost)
# Te same wartości co w Dockerfile / docker-compose (użytkownik, hasło, nazwa bazy).

SECRET_KEY=dev-secret-key-change-for-training
JWT_SECRET_KEY=dev-jwt-secret-change-for-training
FLASK_ENV=development
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

SQLALCHEMY_DATABASE_URI=postgresql+psycopg2://tasks_dev:tasks_dev_secret@localhost:5432/tasks_dev
